services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: placify
      POSTGRES_PASSWORD: placify_dev_password
      POSTGRES_DB: placify
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
